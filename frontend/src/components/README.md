# Components

This directory contains reusable React components.

## Structure

- **common/** - Shared components used across the application (buttons, inputs, modals, etc.)
- **layout/** - Layout components (navbar, sidebar, footer, etc.)
- **features/** - Feature-specific components
